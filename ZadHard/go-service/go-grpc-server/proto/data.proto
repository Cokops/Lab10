syntax = "proto3";

package proto;

option go_package = "./proto";

message DataRequest {
  string timestamp = 1;
}

message DataResponse {
  int32 id = 1;
  string name = 2;
  map<string, string> details = 3;
}

service DataService {
  rpc GetComplexData(DataRequest) returns (DataResponse);
}